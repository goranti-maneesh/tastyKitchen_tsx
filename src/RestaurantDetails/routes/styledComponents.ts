import styled from "styled-components";
import tw from "twin.macro";

export const HomeRouteMainContainer = styled.div``;

export const LoaderViewContainer = styled.div`
    ${tw`min-h-300px flex justify-center items-center text-center`}
`;

export const RestaurantsListMainContainer = styled.div``;
